<%= form_with(model: geography_state) do |form| %>
  <% if geography_state.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(geography_state.errors.count, "error") %> prohibited this geography_state from being saved:</h2>

      <ul>
        <% geography_state.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
