<%= form_with(model: geography_country) do |form| %>
  <% if geography_country.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(geography_country.errors.count, "error") %> prohibited this geography_country from being saved:</h2>

      <ul>
        <% geography_country.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
