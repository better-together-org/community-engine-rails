
<%# locals: (form: ) %>
<div class="nested-fields my-3 row align-items-end" data-new-record="<%= form.object.new_record? %>">
  <%= form.hidden_field :_destroy %>
  <div class="col-md-2">
    <%= required_label form, :label, class: "form-label" %>
    <%= form.select :label, form.object.class.label_options, { prompt: 'Select Label', required: true }, class: "form-select" %>
  </div>
  <div class="col-md-2 mt-3 mt-md-0">
    <%= required_label form, :privacy, class: "form-label" %>
    <%= privacy_field(form:, klass: form.object.class) %>
  </div>
  <div class="col-md-6 mt-3 mt-md-0">
    <%= required_label form, :url, class: "form-label" %>
    <%= form.url_field :url, class: 'form-control', required: true %>
  </div>
  <div class="col-md-2 mt-3 mt-md-0">
    <%= link_to '#', class: 'btn btn-outline-danger btn-sm', data: { action: 'click->dynamic-fields#remove' } do %>
      <i class="fas fa-minus"></i> <%= t('better_together.remove') %>
    <% end %>
  </div>
</div>
