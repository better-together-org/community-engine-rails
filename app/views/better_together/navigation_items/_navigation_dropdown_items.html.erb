<!-- app/views/better_together/navigation_items/_navigation_dropdown_items.html.erb -->

<%# locals: (navigation_item:, level: 0)%>

<ul
  id="<%= dom_id(navigation_item, navigation_item.slug) %>"
  class="dropdown-menu dropdown-menu-end position-absolute <%= dom_class(navigation_item, navigation_item.slug) %>"
  aria-labelledby="<%= navigation_item.slug %>"
  data-bs-container="body">
  <%= render partial: 'better_together/navigation_items/navigation_dropdown_item',
  collection: navigation_item.children, as: :navigation_item, locals: { level: } %>
</ul>
