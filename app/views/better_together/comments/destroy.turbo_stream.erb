<%# The removal is broadcast to every open viewer (including this requester's
    own tab) via BetterTogether::Comment#after_destroy_commit's broadcast_remove_to.
    Nothing further to render here. %>
