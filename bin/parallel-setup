#!/usr/bin/env bash
# Setup parallel test databases for parallel_rspec

set -e

echo "Setting up parallel test databases..."

# Use the official parallel_rspec rake tasks to set up test databases
# These tasks automatically handle TEST_ENV_NUMBER and create numbered databases
echo "Creating and preparing parallel test databases..."
bin/dc-run-dummy bundle exec rake db:parallel:create db:parallel:prepare

echo "✅ Parallel test databases created successfully!"
echo "You can now run: bin/dc-run prspec spec"
