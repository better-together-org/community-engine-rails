default: &default
  url: <%= ENV["REDIS_URL"] || 'redis://localhost:6379' %>
  queues:
    - default
    - mailers
    - high_priority
    - low_priority

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default