# 0.11.0 Asset Capture Plan

This plan tracked the missing `0.11.0` release-packet assets and now serves as a compact completion record for the branch-tip evidence packet.

## Current status

The packet is now complete on branch tip:

- standalone end-user guides exist for export and deletion flows
- release-prefixed privacy screenshots exist
- release-prefixed uploads and image-library screenshots exist
- release-prefixed federation/platform/storage screenshots exist
- E2EE diagram exports exist alongside the source

What remains deferred is public rollout or deeper UX polish work, not missing release evidence.

## Working rules

- Prefer existing screenshot specs when the shipped UI is already covered by a stable review flow.
- Create new release-focused screenshot specs only when the current review specs do not expose the correct page or state.
- Do not invent screenshots for UI that does not exist on the release branch tip.
- For non-UI gaps, add docs or diagrams instead of forcing synthetic capture work.

## 1. Privacy, Member Data Rights, and Consent Auditing

Status: completed

### Added end-user docs

These now exist as standalone guides instead of relying only on release-review screenshots:

- `docs/end_users/my_data_and_exports.md`
  - scope: explain the `My Data` panel, request flow, export statuses, and download behavior
  - source UI: `settings_my_data_path`
  - supporting current screenshot evidence:
    - `pr_1486_my_data_after`
    - `pr_1486_my_data_seeds_after`
    - `pr_1486_person_seed_detail_after`

- `docs/end_users/account_deletion_requests.md`
  - scope: explain the deletion request entrypoint, request lifecycle, cancellation behavior, and review expectations
  - source UI: `edit_user_registration_path`
  - supporting current screenshot evidence:
    - `pr_1486_account_delete_before`
    - `pr_1486_account_delete_after`

### Release-specific screenshots

Status: completed

Use the existing review spec and stable release-prefixed slugs:

- source spec:
  - `spec/docs_screenshots/better_together/person_deletion_review_spec.rb`

- release captures to add:
  - `release_0_11_0_member_data_export`
  - `release_0_11_0_person_deletion_request`

- recommended commands:

```bash
RUN_DOCS_SCREENSHOTS=1 \
CAPTURE_MY_DATA=1 \
MY_DATA_SLUG=release_0_11_0_member_data_export \
bundle exec rspec spec/docs_screenshots/better_together/person_deletion_review_spec.rb
```

```bash
RUN_DOCS_SCREENSHOTS=1 \
ENTRY_SLUG=release_0_11_0_person_deletion_request \
EXPECT_DIRECT_DELETE_BUTTON=1 \
bundle exec rspec spec/docs_screenshots/better_together/person_deletion_review_spec.rb
```

## 2. Content, Media, and Authoring UX

Status: completed

### Uploads gallery

This shipped UI surface now has dedicated release captures.

- route:
  - `file_uploads_path(locale: I18n.default_locale)` which resolves to `/en/f`
- source UI:
  - search field
  - sort selector
  - storage usage summary
  - card grid with `Copy URL` and `Insert`
- target slugs:
  - `release_0_11_0_uploads_gallery`

Implemented with:

- `spec/docs_screenshots/better_together/release_0_11_0_uploads_gallery_spec.rb`

### Block image-library picker

This now has a dedicated release-focused spec. The older `pr_1490` evidence was not enough on its own.

- likely source route:
  - block editor new-form flow for an image-capable block type
- supporting shipped UI:
  - uploads gallery `Insert` button carries the blob `signed_id`
- target slugs:
  - `release_0_11_0_block_image_library`

Implemented with:

- `spec/docs_screenshots/better_together/release_0_11_0_uploads_gallery_spec.rb`

### Existing evidence to reuse

Do not recreate these unless UI drift makes them stale:

- `pr_1490_hero_form_after`
- `pr_1490_checklist_form_after`
- `posts_index_ordering`

## 3. Federation, Platform Connections, and Multi-Tenant Scoping

Status: completed

Screenshots are required for UI-bearing federation and multi-tenant surfaces, and the branch-tip packet now includes dedicated federation/platform/storage captures.

Reason:

- the branch tip already has related member portability UI evidence, but not the full platform-manager/admin visual coverage the release packet should ideally carry
- the most accurate current evidence already present is:
  - `docs/platform_organizers/federation_privacy_and_consent.md`
  - `docs/plans/federation-consent-identity.md`
  - `pr_1486_person_seeds_index_after`
  - `pr_1486_person_seed_detail_after`
  - platform manager diagrams under `docs/diagrams/source/`

Implemented with:

- `spec/docs_screenshots/better_together/release_0_11_0_federation_platform_spec.rb`

Release-prefixed captures now include:

- platform connections index
- platform connection editor
- host-platform profile/admin view
- person platform integrations
- storage configurations index
- storage configuration form

## 4. Metrics and Privacy-Preserving Analytics

No dedicated release screenshot should be required unless a real organizer-facing configuration page lands on the release branch tip.

Current branch-tip evidence is documentation-first:

- `docs/developers/systems/metrics_system.md`
- `docs/implementation/metrics_retention_design_decisions.md`
- `docs/diagrams/source/metrics_flow.mmd`

Action retained:

- keep metrics retention as docs + diagram coverage for `0.11.0`
- no branch-tip organizer-facing retention configuration screen exists to capture

## 5. SEO, Structured Data, and Native Polish

No dedicated release screenshot is required.

Reason:

- these changes are better explained by release prose and the capability map than by static UI screenshots
- the branch tip does not have a specific reviewer-facing screen for JSON-LD/meta-tag output

## Final execution order

Completed:

1. Added the standalone export/deletion guides.
2. Generated release-prefixed privacy screenshots.
3. Generated release-prefixed uploads and image-library screenshots.
4. Generated release-prefixed federation/platform/storage screenshots.
5. Rendered release-required diagram exports including E2EE.
6. Re-ran docs validation and release inventory validation.
